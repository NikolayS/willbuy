
__pycache__/
!.env.example
!.env.op
!scripts/push-secrets.sh
.DS_Store
.env
.env.*
.env.local
.hcloud*
.idea/
.next/
.resend*
.venv/
.vscode/
*.env
*.key
*.log
*.pem
*.pyc
*.swp
*.tsbuildinfo
*credentials*
*secrets*
# allow-list: script consumes secrets via `op run`, no values baked in
# allow-list: template with op:// references only, no values
# logs
# node / build
# os / editor
# python
# secrets — NEVER commit (repo is public)
apps/aggregator/src/willbuy_aggregator.egg-info/
apps/visitor-worker/run-dogfood.ts
build/
coverage/
dist/
node_modules/
scripts/run-visitor-worker-dogfood.ts
Thumbs.db
venv/
